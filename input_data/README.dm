All samples in this directory should be in .fastq.gz format.
To run the samples correctly take the prefix before .fastq.gz and put it in the sample_names.yml file in the IDs list,
Separate the samples in the list by comma.